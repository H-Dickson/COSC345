#pragma once

#include <string>


class MyCalculator {
public:
    // Function to calculate area based on latitude and longitude
    std::string CalculateArea(const std::string& lat, const std::string& lon);
};
